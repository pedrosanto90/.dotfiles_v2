# Discover input identifiers with: swaymsg -t get_inputs
input type:keyboard {
    xkb_layout pt
    xkb_options caps:escape
    repeat_delay 250
    repeat_rate 40
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
}
