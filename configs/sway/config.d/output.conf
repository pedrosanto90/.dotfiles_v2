# Sway enables newly connected outputs automatically. Open the graphical display
# manager with Super+Shift+M, or run nwg-displays, to arrange, scale, rotate,
# enable, and disable monitors. Saved settings are loaded on future logins.
#
# Discover output names with: swaymsg -t get_outputs
# nwg-displays writes persistent rules to ~/.config/sway/outputs and
# ~/.config/sway/workspaces. Manual fallback examples:
# Examples (uncomment and adjust):
# output eDP-1 resolution 1920x1080 scale 1 position 0,0
# output DP-1 resolution 2560x1440@144Hz position 1920,0 adaptive_sync on
